#include "main.h"

/**
 * swap_int - swaps the values of two integers.
 *
 *Return: Always 0 (Success).
 */

int main()
{
    a = 98;
    b = 42;
    std::cout << "first " << first << " second " << second << "\n";
    swap_ints();
    std::cout << "first " << first << " second " << second << "\n";
}
